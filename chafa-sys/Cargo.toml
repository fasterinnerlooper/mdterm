[package]
name = "chafa-sys"
version = "0.1.0"
edition = "2021"
description = "FFI bindings to libchafa"
authors = ["Shafiq Jetha"]
license = "MIT"
build = "build.rs"

[dependencies]
libc = "0.2"

[build-dependencies]
pkg-config = "0.3"

[features]
default = []
